# Core database & config
pymongo>=4.6.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
motor>=3.6.0

# Presentation generation
python-pptx>=0.6.21

# HTTP & HTML parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# LangChain + LangGraph + LLM (OpenRouter via OpenAI-compatible API)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# Web crawling & browser automation
crawl4ai>=0.4.0
playwright>=1.40.0

# Website content cleaning (prasanna/company-extractor)
trafilatura>=1.6.0
readability-lxml>=0.8.1

# Company name / URL discovery
tavily-python>=0.3.0

# Utilities
tenacity>=8.2.0
typing-extensions>=4.0.0

# Ollama LLM client (free cloud model support)
ollama>=0.4.0

# DOCX parsing (for WK T360 template extraction)
python-docx>=1.1.0

# PDF extraction (for RFP parsing)
pypdf>=4.0.0
PyMuPDF>=1.24.0

# OCR fallback for scanned/image-only RFP pages
# (requires Tesseract binary: https://github.com/tesseract-ocr/tesseract)
pytesseract>=0.3.10
Pillow>=10.0.0

uvicorn
fastapi
python-multipart>=0.0.9

# Authentication & security (JWT + bcrypt — replaces Node.js auth server)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Async email (replaces nodemailer)
aiosmtplib>=3.0.0

# Async HTTP (for Zoom API calls)
httpx>=0.27.0

# Google Meet / Calendar OAuth (optional — needed only if Google Meet is configured)
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0
